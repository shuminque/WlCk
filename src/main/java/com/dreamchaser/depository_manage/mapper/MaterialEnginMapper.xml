<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- material_type -->
<mapper namespace="com.dreamchaser.depository_manage.mapper.MaterialEnginMapper">
    <!-- This code was generated by TableGo tools, mark 1 begin. -->
    <!-- 字段映射 -->
    <resultMap id="materialEnginMap" type="com.dreamchaser.depository_manage.entity.MaterialEngin">
        <id column="id" property="id" jdbcType="INTEGER" />
        <result column="ename" property="ename" jdbcType="VARCHAR" />

    </resultMap>
    <!-- 表查询字段 -->
    <sql id="allColumns">
        me.id, me.ename
    </sql>
    <!-- This code was generated by TableGo tools, mark 2 end. -->
    
    <!-- 查询所有数据 -->
    <select id="findMaterialEnginAll" resultMap="materialEnginMap">
        SELECT
        <include refid="allColumns" />
        FROM material_engin me
    </select>
    
    <!-- 根据条件参数查询数据列表 -->
    <select id="findMaterialEnginByCondition" resultMap="materialEnginMap" parameterType="map">
        SELECT
        <include refid="allColumns" />
        FROM material_engin me WHERE 1 = 1
        <if test="ename != null and ename != ''">
            AND mt.ename LIKE CONCAT('%', #{ename}, '%')
        </if>
    </select>
    
    <!-- 根据主键查询数据 -->
    <select id="findMaterialEnginNameById" resultType="string" parameterType="integer">
        SELECT
            me.ename
        FROM material_engin me WHERE me.id = #{id}
    </select>
    <!-- 根据主键查询数据 -->
    <select id="findMaterialEnginById" resultMap="materialEnginMap" parameterType="integer">
        SELECT
        <include refid="allColumns" />
        FROM material_engin me WHERE me.id = #{id}
    </select>
    <!-- 根据主键查询数据 -->
    <select id="findMaterialEnginByIds" resultMap="materialEnginMap" parameterType="list">
        SELECT
        <include refid="allColumns" />
        FROM material_engin me WHERE me.id IN
        <foreach collection="list" index="index" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <!-- 插入数据 -->
    <insert id="insertMaterialEngin" parameterType="map">
        INSERT INTO material_engin (
            id, ename
        ) VALUES (
            #{id},
            #{ename}
        )
    </insert>
    
    <!-- 批量插入数据 -->
    <insert id="insertMaterialEngins" parameterType="list">
        INSERT INTO material_engin (
            id, ename
        ) VALUES
        <foreach collection="list" index="index" item="item" separator=",">
            (
                #{item.id},
                #{item.ename}
            )
        </foreach>
    </insert>
    
    <!-- 修改数据 -->
    <update id="updateMaterialEngin" parameterType="map">
        UPDATE material_engin
        <set>
            <if test="ename != null">
                ename = #{ename},
            </if>
        </set>
        WHERE id = #{id}
    </update>
    
    <!-- 批量修改数据 -->
    <update id="updateMaterialEngins" parameterType="list">
        <foreach collection="list" index="index" item="item" separator=";">
            UPDATE material_engin
            <set>
                <if test="item.ename != null">
                    ename = #{item.ename},
                </if>
            </set>
            WHERE id = #{item.id}
        </foreach>
    </update>
    
    <!-- 根据主键删除数据 -->
    <delete id="deleteMaterialEnginById" parameterType="int">
        DELETE FROM material_engin WHERE id = #{id}
    </delete>
    
    <!-- 根据主键批量删除数据 -->
    <delete id="deleteMaterialEnginByIds" parameterType="list">
        DELETE FROM material_engin WHERE id IN
        <foreach collection="list" index="index" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
    <select id="findCountByCondition" parameterType="map" resultType="integer">
        SELECT
        count(*)
        FROM material_engin d WHERE 1=1
        <if test="ename != null and ename != ''">
            AND d.ename LIKE CONCAT('%', #{ename}, '%')
        </if>
    </select>
</mapper>