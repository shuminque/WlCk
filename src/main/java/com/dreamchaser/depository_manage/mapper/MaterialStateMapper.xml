<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- material_type -->
<mapper namespace="com.dreamchaser.depository_manage.mapper.MaterialStateMapper">
    <!-- This code was generated by TableGo tools, mark 1 begin. -->
    <!-- 字段映射 -->
    <resultMap id="materialStateMap" type="com.dreamchaser.depository_manage.entity.MaterialState">
        <id column="id" property="id" jdbcType="INTEGER" />
        <result column="sname" property="sname" jdbcType="VARCHAR" />
    </resultMap>
    <!-- 表查询字段 -->
    <sql id="allColumns">
        mt.id, mt.sname,
    </sql>
    <!-- This code was generated by TableGo tools, mark 2 end. -->
    
    <!-- 查询所有数据 -->
    <select id="findMaterialStateAll" resultMap="materialStateMap">
        SELECT
        <include refid="allColumns" />
        FROM material_state mt
    </select>
    
    <!-- 根据条件参数查询数据列表 -->
    <select id="findMaterialStateByCondition" resultMap="materialStateMap" parameterType="map">
        SELECT
        <include refid="allColumns" />
        FROM material_state mt WHERE 1 = 1
        <if test="sname != null and sname != ''">
            AND mt.sname LIKE CONCAT('%', #{sname}, '%')
        </if>
    </select>
    
    <!-- 根据主键查询数据 -->
    <select id="findMaterialStateNameById" resultType="string" parameterType="integer">
        SELECT
        mt.sname
        FROM material_state mt WHERE mt.id = #{id}
    </select>
    <!-- 根据主键查询数据 -->
    <select id="findMaterialStateById" resultMap="materialStateMap" parameterType="integer">
        SELECT
        <include refid="allColumns" />
        FROM material_state mt WHERE mt.id = #{id}
    </select>
    <!-- 根据主键查询数据 -->
    <select id="findMaterialStateByIds" resultMap="materialStateMap" parameterType="list">
        SELECT
        <include refid="allColumns" />
        FROM material_state mt WHERE mt.id IN
        <foreach collection="list" index="index" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <!-- 插入数据 -->
    <insert id="insertMaterialState" parameterType="map">
        INSERT INTO material_state (
            id, sname
        ) VALUES (
            #{id},
            #{sname}
        )
    </insert>
    
    <!-- 批量插入数据 -->
    <insert id="insertMaterialStates" parameterType="list">
        INSERT INTO material_state (
            id, sname
        ) VALUES
        <foreach collection="list" index="index" item="item" separator=",">
            (
                #{item.id},
                #{item.sname}
            )
        </foreach>
    </insert>
    
    <!-- 修改数据 -->
    <update id="updateMaterialState" parameterType="map">
        UPDATE material_state
        <set>
            <if test="sname != null">
                sname = #{sname},
            </if>
        </set>
        WHERE id = #{id}
    </update>
    
    <!-- 批量修改数据 -->
    <update id="updateMaterialStates" parameterType="list">
        <foreach collection="list" index="index" item="item" separator=";">
            UPDATE material_state
            <set>
                <if test="item.sname != null">
                    sname = #{item.sname},
                </if>
            </set>
            WHERE id = #{item.id}
        </foreach>
    </update>
    
    <!-- 根据主键删除数据 -->
    <delete id="deleteMaterialStateById" parameterType="int">
        DELETE FROM material_state WHERE id = #{id}
    </delete>
    
    <!-- 根据主键批量删除数据 -->
    <delete id="deleteMaterialStateByIds" parameterType="list">
        DELETE FROM material_state WHERE id IN
        <foreach collection="list" index="index" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>